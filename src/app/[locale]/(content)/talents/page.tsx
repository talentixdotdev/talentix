export const dynamic = "force-static";

const ForCompaniesPage: React.FC = () => {
	return (
		<div className={"flex flex-col items-center min-h-screen sm:gap-y-20"}>
			For companies
		</div>
	);
};

export default ForCompaniesPage;
